<?php
$servername = "localhost";
$username   = "root";
$password   ="";
$database   ="db_latihan";

$conn = mysqli_connect($servername, $username, $password, $database);

$sql = "SELECT * FROM data_user";
$hasil = $conn->query($sql);
while($baris = $hasil->fetch_assoc()) {
    $user_db = $baris['nama_barang'];
    $pass_db = $baris['harga'];

    echo $nama_db;
    echo $harga_db;
    break;

}

?>